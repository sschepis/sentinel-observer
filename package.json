{
  "name": "sentient-observer",
  "private": true,
  "version": "0.1.0",
  "description": "Sentinel: a sentient-observer learning app. Monorepo: @sschepis/sentient-core (cognitive engine) + web app.",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "dev": "npm run dev --workspace @sschepis/sentinel-web"
  },
  "license": "MIT"
}
