# Sentinel Observer — task list

*Updated 2026-09-09. Companion to ANALYSIS.md (what was wrong), IMPROVEMENT_PLAN.md (what to do about it), NULL_ARMS.md (the memory-substrate measurements) and SYNTHETIC_MIND.md (the gap analysis from here to the stated goal; its tasks 39–58 are listed below).*

## Where things stand, in plain terms

The code was audited against the paper. Eleven real bugs were fixed and merged to `main` with tests. Three mechanisms the paper describes but the code never ran — the drives, the deviation meter as a reading of speech, and the goal loop — are now wired in, each with a bench that shows the mechanism moving something. A benchmark was built that compares the observer's memory against simpler versions of itself, and it was run both on fresh vocabulary and on the live observer's own learned record. The result: the oscillator-field similarity term in memory scoring ("the SMF term") does not help recall — a plain index over the prime signatures recalls at least as well and separates real cues from near-miss distractors better. The field's coupling does nothing for recall either. Switching to index-only scoring is a one-line configuration change that does **not** require retraining the live observer, but every confidence threshold in the system was tuned on the old score scale and must be re-fitted first. One existing test already fails for exactly that reason (a threshold, not a behavior).

Nothing done so far has touched the running server or its learned record. Everything below lands on the live observer the next time the server restarts (decision #14: all at once).

Two terms used below: **SMF term** = the oscillator-field similarity term in the memory score. **Index-only** = scoring memories by prime-signature overlap alone (`OBSERVER_SMF_WEIGHT=0`).

---

## Done

| # | Task | Where |
|---|---|---|
| 1 | Audit the code against the paper | `docs/ANALYSIS.md` |
| 2 | Turn the findings into a plan with general rules | `docs/IMPROVEMENT_PLAN.md` |
| 3 | Fix the day-one defects (11 of 20) with regression tests; merge to `main` | commit `835b745` |
| 4 | Build the null-model benchmark for the memory substrate; run on 200/1,000 fresh words | `nullArmsBenchmark.test.ts`, `bench/null-arms/*-w*.json` |
| 5 | Run the benchmark read-only against the live observer's record | `bench/null-arms/*-record.json` |
| 6 | Add the `OBSERVER_SMF_WEIGHT` / `OBSERVER_COUPLING` switch (applies to server, trainer and all gates; never re-encodes stored memories) | `observer/engine.ts` |
| 7 | Run the heavy gates with the SMF term off: semantic recall and CI gates unchanged, polysemy honesty holds, one sense-split threshold fails | `docs/NULL_ARMS.md` |
| 9 | Sense-disambiguation threshold made scale-free (runner-up overlap ÷ winner overlap ≥ 0.5, instead of a fixed 0.17 margin) | `teacher/cde.ts`, commit `ead9c57` |
| 10 | Recall floor made a calibrated gate; `refit-gates` fits floor and high-confidence gate from the live record with a held-out split; server loads the fitted values from `OBSERVER_GATES_FILE` and refuses an artifact fitted under a different arm | `teacher/calibration.ts`, `cli/refit-gates.ts`, commits `ead9c57` `8bcd711` — control fitted: high-confidence 0.794 (hand 0.8), floor 0.749 (hand 0.6). Index-only re-run still pending (see #10 below). |
| 14 | Decided: take all the day-one fixes at once on the next restart | — |
| 16 | Drives now own one real decision: when a reply could be a composition about known material or a question about it, the drives choose (sampled at the drive temperature). Bench: ask share rises from 5.6% to 44.4% as curiosity goes 0→1; the same drive state on a fresh teacher reproduces the same shares exactly. | `teacher/agent/motivation.ts`, `driveArbitration.test.ts`, commit `8e35fca` |
| 17 | Deviation meter now reads what was *said*: a question or "I cannot tell" is an abstention; a claim that cites memory (traces, edges, an operator, rules) is grounded; a claim that cites nothing is composed — whichever layer produced it. A read-about fact spoken at the ask layer ("Zeus is a god") now counts as grounded speech and carries the edges it came from. Shown on the introspection page and as a chat badge only where it disagrees with the routing label. | `teacher/speechAct.ts`, `speechAct.test.ts`, commit `58968e7` |
| 28 | **The grader check.** Found while reading the live record after the restart: the observer has had 2,619 compositions graded and *zero* graded good, ever — its compose drive weight was pinned at the floor by a judge that may never have said "good". The training loop now grades known-good and known-bad answers (taught pairs' own responses vs. other pairs') before its first cycle and every 100 cycles; a grader that cannot separate them, or never grades a correct answer as strong, is *untrusted*: its grades are recorded but move nothing (no reinforcement, weakening, drive outcome or gap), and creative practice stops asking it. Verdict on the introspection page and in the chat feedback. `npm run grader-check` prints the distributions for the configured model. | `teacher/graderCheck.ts`, `graderCheck.test.ts`, `cli/grader-check.ts`, commit `82137b7` |
| 29 | Goal stalls and completions were booked twice (seen live: 6 abandoned for 3 stalls). One transition, one entry. | commit `661822c` |
| 18 | Goal loop runs in the server: every classroom cycle discovers goals from the observer's own measures (words it keeps missing → learn-word; "I keep failing X" → fill-gap), takes one plan step on a goal chosen at the drive temperature, and a goal that stalls raises its target in the lesson queue and makes it the next research topic. Bench: a stalled goal on the last-ranked word puts it first; without the stall the order is unchanged. | `teacher/agent/goals.ts`, `teacher/plan.ts`, `server/trainingLoop.ts`, `goalLoop.test.ts`, commit `7ed83f5` |

---

## Next: switch memory scoring to index-only, safely

Do these in order. Nothing is flipped on the live server until step 12.

| # | Task | Who | What it involves |
|---|---|---|---|
| 8 | Measure the new score margins | Sebastian | `cd apps/web && OBSERVER_SMF_WEIGHT=0 npm run cde-bench` — prints the margin bands for exact cues vs distractors under index-only scoring; paste the output back. |
| 10 | Re-run the gate fit under index-only scoring | Sebastian | `cd apps/web && OBSERVER_SMF_WEIGHT=0 npm run refit-gates` — the first run fitted both gates at 1.000 (a knife edge: every non-exact cue scored below every exact one). The fit now includes legitimate variants of taught cues as positives, so the gates land where variants still pass. Paste the output. Writes `bench/calibration/conversation-gates-smf-off.json`. |
| 11 | Re-run everything under index-only scoring | Sebastian | `npm test`, the three heavy gates, and the null-arm bench in record mode. All green = go. |
| 12 | Flip the live server | Sebastian | Add `OBSERVER_SMF_WEIGHT=0 OBSERVER_GATES_FILE=../../bench/calibration/conversation-gates-smf-off.json` to the server's environment and restart at a quiet moment. No retraining. Watch the report card (λ, ask share, deviation meter, goal steps/stalled on the introspection page) for a day. |
| 13 | Rewrite the paper's memory sections from the bench artifacts | Claude | §3.1, §5.1, §5.2: what recall actually computes, with the null-arm tables and their n. |

---

## Before the next server restart (independent of the switch)

The merged fixes change behavior the first time the server restarts. Decide these before that happens.

| # | Task | Who | Note |
|---|---|---|---|
| 14 | ~~Decide: take all day-one fixes at once, or stage them~~ Decided: all at once. | Sebastian | What changes on the first restart: learned n-gram and drive weights decay for the first time (a one-time step down, then steady); the composite is re-scaled so λ starts moving off zero; negations need an explicit known subject; the drives start choosing between composing and asking about known material; the classroom loop starts forming and pursuing goals (new "goal" events in the learning stream; "goal steps / completed / stalled" on the introspection page). |
| 15 | Audit the live confirmed-false store for junk | Claude | Read-only, from the snapshot: list every stored "X is not a Y" that the new negation guard would have refused (idioms, pronoun subjects). You decide what to prune. |

---

## Then: the other three tracks you chose

| # | Track | Task | Note |
|---|---|---|---|
| 16–18 | Dormant mechanisms | Done — see the Done table. Left open in this track: practice and verify-belief goals are not formed automatically (their plans quiz a deck word but their completion reads a conversation phrase or a belief, so they would stall on construction — a fake curriculum signal); fixing those plan shapes is a small follow-up. | |
| 19 | Field | Split the memory sketch into a content sketch and a context sketch; bench context-cued recall ("what did we cover around X?") against a null | Turns the recency signal from a confound into a feature. |
| 20 | Field | Prototype the resonant readout (excite the top candidates, let inhibition pick) against a softmax null on the polysemy sibling set | The last remaining way the field could earn a job in recall. If it loses, the paper says "encoder". |
| 21 | Procedures | Replace the drill-name→template lookup with a signature-selected enumerator over a small term language; instruction and chaperone proposals become front-ends into the same candidate space; honest MDL cost for numerals | Turns "one gcd example" into "k of 36 families induced". |
| 22 | Procedures | Fix the story parser's misreads and the logic prompt grammar; add adversarial instruction tests | Listed in ANALYSIS.md §5.3. |
| 23 | Benches | Every bench writes a JSON artifact; paper tables are generated from them; honesty-bench negatives come from WordNet instead of the model's own graph; seed the fuzz bench | Rule 3 and Rule 5 from the plan. |

---

## After the grader check (new, in order)

| # | Task | Who | Note |
|---|---|---|---|
| 30 | Run `cd apps/web && npm run grader-check` against the current teacher model and paste the output | Sebastian | The model in the ledger is `dirty-muse-writer-v01-uncensored-erotica-nsfw-i1` — a fiction-writing model, not an instruct model tuned for JSON grading. The check will say whether it can judge at all. |
| 31 | If untrusted: point `OBSERVER_CHAPERONE_MODEL` at an instruction-tuned model that returns the grade schema; re-run the check; restart | Sebastian | The same model also answers gaps and proposes exchanges — the stalled fill-gap goals on creative-practice prompts are probably the same failure. |
| 32 | Restart the server once more to pick up #28/#29 | Sebastian | Until then the running process still books stalls twice and applies every grade. |
| 33 | Watch `compose` in behaviorWeights drift back toward 0.3 once no fresh compose outcomes land (driveWeightDays) | — | If the grader is repaired instead, wins will move it directly. |

## Training material (new — docs/CURRICULUM.md)

| # | Task | Who | Note |
|---|---|---|---|
| 34 | Curriculum pipeline: registry, budgeted feeder with persisted cursors, held-out slice; ConceptNet → relations (+ its Not* claims as confirmed-false), DailyDialog → pairs, Simple Wikipedia / TinyStories → passages, SVAMP / ASDiv → checkable problems | Claude — done | commits `87a526e` `8abe75b` `aba0c70`. Scale bench: 1,000-row feed ≈ 1 s; question latency unchanged to 110k edges. |
| 35 | Fill the corpus: `npm run fetch-conceptnet`, then `npm run fetch-hf -- dailydialog / simplewiki / tinystories / svamp / asdiv` | Sebastian | Run on the Mac; the server never downloads. Paste the per-relation counts from ConceptNet and the `kept` counts from the others. |
| 36 | Run the source benches before the server eats a corpus: `npx jest -c jest.bench.config.cjs --testPathPatterns passageBenchmark` (parse rate + claim sample per prose source) | Sebastian, then Claude reads | Decides the passage budget; narrative is expected to lose to encyclopedia prose. |
| 37 | Definitions source (Wiktionary glosses for deck words without one) and a story-state engine for bAbI-style questions | Claude | The two shapes without a fetcher / answering path yet. |
| 38 | Held-out edge recovery bench over the real ConceptNet file | — | Folded into #40 below. |

## The synthetic mind (new — docs/SYNTHETIC_MIND.md)

The goal, stated so it can fail: a system that self-organizes and learns in real time, more like a person than like current AI training, that says it does not know when it does not know, and never asserts what it cannot derive from what it has stored. The principle — coupling lowers entropy by opening a channel between elements — is pinned to the observer as built: elements are concepts, channels are corroborated edges, phase difference is the contradiction ledger, entropy is the uncertainty of the observer's own answers, and self-organization is choosing what to couple next. The prediction that tests it: the learning steps that lower that entropy most are the ones after which the observer does best on material it never saw. The one-shot bench (43) and the prediction bench (41) are the two numbers that decide whether the mind is forming. Full analysis and acceptance criteria in SYNTHETIC_MIND.md §5; order in §4.

**Phase 0 — measure (readout only; safe for the live server)**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 39 | Network entropy readout: `networkEntropy()` over the observer's own answers per concept, logged per learning step, in introspection | Claude — **done** | `teacher/networkEntropy.ts` + test (6 green). Training loop reads it every 5 cycles (`entropy` / `entropyDelta` in stats, an `entropy:` line in the stream); `introspection().trust.entropy`; Introspect view shows total, per-concept mean, slot states and the most uncertain concepts. Slot ladder: certain 0.29 bits · single-source 0.81 · inherited 0.88 · weakened 0.93 · conflicted / unknown 1.00. |
| 40 | Held-out ConceptNet edge-recovery bench (was #38), by relation and by path (direct / inherited / graded layer) | Claude — built; run in progress | `curriculum/heldOutRecoveryBenchmark.test.ts` (bench config; `RECOVERY_STEPS/BUDGET/PROBES/TAUGHT` env; resumable with `RECOVERY_STEPS_PER_RUN`, checkpoint under `bench/curriculum/.recovery-state/`). Probes: held-out rows with both ends among the taught words; plus a **seen-claim control** (rows from the first feed). First 16 steps on the shuffled corpus: seen-control 12% → 68% after one feed (ingest → answer works); held-out 8.0% → 9.3% after 16k rows, the gains all hedged (graded layer); mean entropy 10.156 → 9.918 bits/concept over the 2,000 taught words. Three things it found on the way are tasks 59–61. |
| 41 | The prediction: Δ entropy per feeder step vs held-out recovery, with "edges added" as the null predictor | Claude — **run; the prediction failed as stated** | Shuffled corpus, 65 × 1,000 rows, 200 probes (both ends taught): recovery 8.0% → 38.0% (0 wrong in 13,000 probes; 40 is-a chains, 31 inherited, 5 direct, graded layer 0; is-a 38/90, located-in 20/38, has-part 7/20, used-for 6/18), mean entropy 10.156 → 9.424. ρ(Δentropy, Δrecovery) = −0.057 per step, slot-matched +0.032; in blocks of 5 steps **+0.39** — the wrong sign. The entropy drop per block shrinks steadily (−0.072 → −0.019 bits over the run: a concept's own slots fill early and a second edge into a filled slot moves nothing) while recovery gains per block stay flat (4–6 probes per 5,000 rows) — recovery grows with chain *density*, which the slot measure does not see. The null predictor (edges added) was −0.41 for the same reason (edges/feed fall as duplicates rise). Verdict: the measure counts what is stored, the prediction is about what is derivable; task 39's entropy must be redefined over the inferential closure (what the observer can derive about a concept, not what it holds directly) before the prediction can be tested again. Artifact `bench/curriculum/held-out-recovery-2026-09-09-s65x1000-p200.json` (shuffled; the ordered-file run is the earlier commit). |
| 42 | Soundness audit gate: every assertion in every bench has provenance that exists and entails the claim; CI fails on the first that does not | Claude — **done, green** | `teacher/soundness.ts` (the invariant, per layer; the graded layer counts when the answer is hedged and the distributed-vector score re-checks above the operators' floor) + `soundnessGate.test.ts`: 476 answers audited · 80 derived · 12 structural · 384 abstained · 0 dangling · 0 unbacked. **Its first run found a real defect**: a Markov fallback sentence spoken as a flat assertion ("tell me about the farm" → "Tell me about the farm."). Fixed: an ungrounded composition is now spoken inside a decline that names it as word-play (`speakUngrounded`, speechAct.ts); the `grounded: false` flag and the grounding score are unchanged. |
| 43 | One-shot learning bench — the person test | Claude — **done; 1/6 today** | `teacher/oneShotLearningBenchmark.test.ts` (bench config), writes `bench/one-shot/latest.json`. First run: shape 1 fails (a declarative typed in chat is not read into an edge → task 44); shapes 2, 4, 5 blocked by 1; **shape 3 fails as WRONG** — "no, a cow does not have a tail" is not parsed by the negation grammar (it wants the bare "X does not have Y"), so the correction is asked back and the old answer stands → task 45; control (no unearned Yes) passes. |

**Phase 1 — conversation as a learning channel**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 44 | Read declaratives typed in chat into edges (class `conversation`, reader precision guards, vocabulary growth for an unknown subject) | Claude | bench 43 shape 1; reading precision and adversarial honesty unchanged |
| 45 | Corrections and confirmations in chat are world-feedback grades against the answer's provenance; promote matching hypotheses (closes ANALYSIS #11) | Claude | bench 43 shape 3 |
| 46 | The guess mode: an assertion carrying its derivation and a question, where an inference path exists; fourth meter category `guessed`; confirmation → corroborated edge, denial → negation | Claude | bench 43 shape 4; honesty gates unchanged |
| 47 | Analogy over shared edges (generalized prototype bundle), always labeled as a guess | Claude | recovery bench reports analogy separately |

**Phase 2 — disagreement and coupling drive the dynamics**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 48 | Conflict sweep in the live loop; verification as `verify-belief` goals; conflicts as a term of the entropy | Claude | bench 43 shape 5; conflicts fall over a corpus run |
| 49 | Consolidation by coupling (corroborated or cited in correct answers, unweakened) instead of the substrate-entropy lock; old criterion kept as a mode | Claude | consolidation tests pass under both; recall gates unchanged |
| 50 | Earned forgetting: live budgeted near-duplicate merge and pruning of never-cited traces, never consolidated ones | Claude | held-out recovery does not fall after a prune |

**Phase 3 — entropy in charge of self-direction**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 51 | Goal value = expected entropy drop × success; practice and verify-belief goals formed; introspection explains goals in bits | Claude | all four goal types complete on the corpus |
| 52 | Feeder budgets by measured entropy yield per source (bandit, 10 % floor) | Claude | shares move; recovery per source not worse |
| 53 | Concept synthesis admitted only when it lowers the network entropy; induced concepts speakable | Claude | an answer uses one |

**Phase 4 — the substrate's job**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 54 | Field-as-attention bench: converged-field excitation vs entropy ranking vs curriculum as the choice of what to learn next, over 200 cycles | Claude | one artifact, one decision in NULL_ARMS.md |

**Phase 5 — the honesty claim as a theorem**

| # | Task | Who | Acceptance |
|---|---|---|---|
| 55 | State soundness-relative-to-the-store formally in the paper (§3.7) with its scope and 42's audited count; say plainly that hallucination-free is not error-free | Claude drafts, Sebastian approves | text merged |
| 56 | Design ledger (task 13 reshaped): paper mechanism → term of the principle → bench → status | Claude | every mechanism appears once |

**Found by Phase 0 — fix before the corpus reaches the live server**

| # | Task | Who | Note |
|---|---|---|---|
| 59 | **Corpus order.** `fetch-conceptnet` wrote the dump in its own order — 5,300 Antonym rows first, then AtLocation, …, the 42k IsA rows between rows 20k and 63k — so the feeder would teach every antonym alphabetically before one is-a edge. Fixed: the fetcher now writes a seeded shuffle; `npm run shuffle-corpus -- corpus/conceptnet.en.jsonl` shuffles an existing file in place (keeps `.unshuffled`). | Sebastian: run the shuffle **before** the server ingests the file (the cursor is a row index) | done in code; the operator step remains |
| 60 | **Ingest scaling.** Was: at 26k edges / 26k words a rebuild cost ≈1.5–2 s warm (≈8 s cold) and a 1,000-row feed ran several rebuilds — 38 s per feed under load. **Fixed** (commit after 1fa0027): the strict and loose definition extractions are memoized on (definitions version, vocabulary size); the hologram re-binds only subjects whose edge set changed (≈800 of 18k per feed, with per-subject fingerprints); the hypothesis refresh uses a key set instead of scanning negations per edge; the ConceptNet attestation bumps are recorded before the one reseed. Measured on the same record: a 1,000-row feed **250–390 ms** at 29k edges (rebuild ≈150 ms, two per feed — growth still forces one via `storeNegation → edgeStrengthOf`). | Claude — done | `OBSERVER_PROFILE_REBUILD=1`; `corpusProfile.test.ts` (bench config) builds the record and times it |
| 61 | **Question latency at corpus scale.** Was ≈1–2 s in the sandbox. With the indexed chain walks and an idle machine: **120–240 ms per closed question at 30k edges / 28k words**, of which ≈130 ms is `respond()` (field excitation + settle + recall — the constant cost, same as without the corpus) and the operators ≈1–12 ms. The 1–2 s was contention plus the O(edges)-per-call chain scans. Resolved; `OBSERVER_PROFILE_CHAT=1` prints per-stage laps. | Claude — done | |
| 62 | **Graded recall over ingested knowledge.** The hologram binds the definition-extracted loose graph and the asserted graph, but the recovery bench shows the graded layer contributing almost nothing on ConceptNet-only knowledge; check that ingested edges' subjects are bound (they should be — they are in `relationsCache`) and why held-out siblings do not score. | Claude | after 60 |

**Mathematical reasoning (measured 2026-09-09 — docs/SYNTHETIC_MIND.md §2 row "rewrite engine")**

What it can do today: exact arithmetic over the naturals through the rewrite engine (Peano and digit decks; add, subtract, multiply, exact divide, remainder, powers, exact percent, exact square root, gcd/lcm, place value, rounding, comparison, parity, factors), exact rationals and decimals (`rat.*`, normalised by gcd after every step — 0.1 + 0.2 is 0.3, and a value with no exact decimal keeps its fraction), signed integers for absolute value and temperature, one-unknown equations `x + c = r` and `a·x = r` with integer solutions, unit conversions whose multipliers are induced from drills rather than authored, and propositional logic (and/or/not/if, syllogism) — every answer carrying a derivation trace, every underivable prompt declined. Rules can be taught in chat or induced from graded drills (MDL-scored, held-out checked); a wrong grade weakens an acquired rule. What it cannot do: money with cents (the rationals are exact but ride unary numerals — #81), negative results outside the int deck, and word problems beyond the shapes in #64 and #68 — a net of rates (#82), a comparison against a derived quantity, a transfer between two owners, geometry and proportion.

| # | Task | Who | Note |
|---|---|---|---|
| 63 | **Word-problem bench and the honesty fix.** `curriculum/problemsBenchmark.test.ts` (bench config) samples `corpus/problems.jsonl` (SVAMP + ASDiv, 3,004 rows) and reports right / wrong / abstained by source, by what each problem needed, and — since the story engine landed — by WHICH LAYER answered (mode, operator kind, cited rule ids), because a wrong answer is only fixable once you know where it came from. **First run: right 5 · wrong 19 · abstained 276** — the general story parser added or multiplied the first two numbers of the first sentence on comparisons, residuals, rates, divisions asked as a number of groups, part-of-a-whole and unknown-start stories. Fixed in `rules/parse.ts` (`parseGeneralStory` now counts quantities over the whole body and declines comparison / share / remainder / unknown-start questions, stated wholes and differences, equal groups whose question counts the groups, sums with no join between the numbers, sums asked about one of two kinds, and sums asked as a third kind without a total cue). **After: right 15 · wrong 0 on the same 300; right 10 · wrong 0 on the next 300.** | Claude — done | `PROBLEMS_SAMPLE`, `PROBLEMS_SKIP`, `PROBLEMS_SHOW`, `PROBLEMS_TAUGHT`; artifacts `bench/curriculum/word-problems-<date>-n<N>[-skip<K>].json` |
| 64 | **A story-state engine** (was #37/#22) | Claude — **done** | `teacher/rules/story.ts` (+ `story.test.ts`, 69 green). A word problem is read as a STATE: every number becomes a quantity with the noun it counts, the words that distinguish it (by slot: modifier, of-phrase/tail, pre-verbal, time), an owner, and a role — gained, lost, a later state or stated whole, an equal-groups rate, a group count. The question is read by matching its words against that story, and its kind is one of total / residual / start / change / difference / share. Two rules keep it sound: **every quantity is accounted for** (used in the derivation, or ruled out because the question named a different owner or a contrasting word in the same slot — a number nobody can place means decline), and **the selection is unambiguous** (two candidates for one side of a difference → decline). The arithmetic must close in ℕ: no negative intermediate, only exact division. **Measured over all 3,004 problems: 407 answered, 407 right, 0 wrong** — and the rewrite engine derives every one of those readings (checked term by term, the engine's value always equals the reading's). Shapes: total 103, change 93, residual 51, per×groups 42, start 24, groups=whole÷per 23, difference 20 + 6 by owner + 2 of two kinds, share 15, whole−parts 1. Through the full chat path the bench went **5 right / 19 wrong → 53 right / 0 wrong** (first 300) and **40 right / 0 wrong** (next 300). Two capability limits it found on the way are #69 and #70. |
| 65 | **Ask the store before summing kinds** | Claude — **done** | `rules/story.ts` takes a `StoryWorld` — `isKindOf` / `hasPart` over the observer's own is-a graph, with its denials, walked by the same `chain.ts` functions the answer layer uses — and `agent/rules.ts` hands it one built from the live store. "58 geese and 37 ducks — how many BIRDS in all?" is now derived, and the reading records WHY: `goose is-a bird, duck is-a bird`. A silent store still declines, and a part question is never a kind question however generous the store ("35 elephants and 48 tigers — how many LEGS" needs legs-per-elephant, which no is-a chain holds). Measured against the real ConceptNet corpus: **7 more problems answered, 407 → 414, still 0 wrong** — the unlocked ones are girls+boys→children (×3), geese+ducks→birds, roses+carnations→flowers, sheets→pieces of paper, friends→people. Small in count and the whole point in kind: two ingested facts turn a decline into a derivation, which is the coupling claim in miniature. |
| 66 | **Vocabulary is the first wall — but not for arithmetic** | Claude — **done** | Measured after #64: the wall does NOT block the story engine, because the rewrite dispatch runs before the vocabulary ask, so every story the engine reads is answered whatever words it contains. What was left was the honesty of the ask itself, and it was wrong: `extractUnknownSubject` read a NUMERAL as the utterance's unknown referent — "I do not know what \"twenty-three\" means. Could you teach me?" was a real reply to a word problem. Digits, number words (whole or hyphenated) and common verbs are no longer offered as things it could be taught. |
| 67 | **Rationals and decimals deck** — done. `teacher/rules/rat.ts`: a value is `rat.q(numerator, denominator)` over two nat numerals and every operation is a RULE, not a native. `rat.norm` divides both parts by their gcd after every step, so a value has one representation, `rat.eq` is decidable and the numerals stay small enough for unary arithmetic. **`nat.gcd` is authored here for the first time** (Euclid — the example the engine's lazy `ite` was built for; an eager strategy diverges on it), and it had been referenced by the drill parser and authored nowhere. `rat.div` by zero reduces to `rat.undefined`, a symbol with no rules and no decoding — so it is STUCK, which is how the engine declines instead of inventing a number; a subtraction below zero leaves `nat.sub` stuck the same way. Measured exact: **0.1 + 0.2 is 0.3** (654 steps), 1/2 + 1/3 is 5/6, 7 ÷ 2 is 3.5, 2.5 − 7 stuck, 5 ÷ 0 undefined. `decodeNormalForm` speaks a whole number as itself, an exact decimal where one exists, and otherwise the FRACTION — never a rounded decimal the observer did not derive — and `checkProblem`'s `numberIn` now reads "5/6" as a number so an exact answer cannot be graded wrong. Wired into the story engine: decimals are quantities (the `\d\.\d` decline is gone), the deck follows the tree (`deckFor`: whole at every step → digits or Peano, otherwise rat), and two new soundness rules keep the meaning honest — **a count cannot be fractional** ("$1.20 in quarters and dimes — how many coins?" reads 1.2, which is not a number of coins: decline) while "how much" may be, and division is whole-only unless the thing asked for is money or a measure. **Measured over all 3,004 problems: 418 answered, 418 right, 0 wrong** (was 414; 411 without the relation store), every reading still derived term by term by the engine. | Claude — **done** | `rat.test.ts`; story fuel 200k for a rat term, because the gcd is Euclid over unary numerals |
| 68 | **Multi-step composition, and the rate stories it unlocked** — done. A reading's term can now be deeper than one operation: the count a rate multiplies is the STORY the remaining quantities tell about the groups, so "Kaleb bought 14 boxes and gave 5 to his little brother; each box has 6 pieces inside — how many pieces did he still have?" is ONE term two operations deep, `dig.mul(dig.sub(14, 5), 6)`, decided by the narrative's own soundness rules rather than stitched together from two answers. Four other honest observations came out of measuring it, and each is worth more than the composition itself: (1) **a measure is not one of the alternatives** — the rate was being counted as one more unnamed candidate in the contrast test, so EVERY two-kind rate story declined ("28 packages of gum and 13 of candy, 4 pieces in each package — how many pieces of gum?"); (2) **the most specific named slot decides** — a question can name words in several slots and they are not equal ("the Ferris WHEEL has 2 SMALL seats and 23 LARGE seats … how many people on SMALL seats" was reading the whole wheel, because 'wheel' looked as discriminating as 'small'); (3) **a quantity that counts the group is the number of groups, not a rate** ("placing her pencils into 19 ROWS with 4 pencils in each ROW" was three rates and no count); (4) `groupNounOf` never read "each of HIS 29 bookshelves", because the determiner after "of" became the group's name. New declines, each a real limit: two rates the question names BOTH of ("53 flowers and 181 sticks per pot — how many flowers and sticks in all"), two amounts of money over the same groups (an income and a cost are a NET, not a choice — "$491 a week … pays $216 rent a week"), an unnamed ADDITION to a rate story ("17 pizzas, 4 slices each, if there are 25 of us"), and a comparison against a derived quantity ("(14 − 4) − 9"). **Measured over all 3,004 problems: 456 answered, 455 right** — and the one "wrong" is a corpus error we checked by hand (row 76: 28 + 14 packages × 6 pieces is 252; the corpus says 7, which is 42 ÷ 6). Was 418. | Claude — **done** | `story.test.ts` 92 green; the authored-deck guard means the bad row weakens nothing — it stores a belief that the parse or the grade is suspect, which is exactly right |
| 82 | **A net of rates over the same groups** — done. `(Σ income − Σ cost) × groups`, with the sign taken from the clause that stated each rate: the reader was throwing away whether a RATE's clause was a loss (the rate role overwrote it), so "Lewis earns $491 every week during the 1181 weeks of harvest. If he has to pay $216 rent every week" had no way to be anything but a choice between two numbers. `StoryQuantity.paid` keeps it, and the shape reads only when the question ACCOUNTS FOR EVERY RATE — by naming what each one counts ("53 flowers and 181 sticks in each pot — how many flowers and sticks in all") or by asking for money when every rate is an amount of money. Two guards came out of measuring it: **rates that count the same thing are not a net** ("10 red peaches and 2 green peaches in each basket — how many GREEN peaches" contrasts by its words, so the question CHOOSES; adding them answered 84 where the story says 14), and the matching positive rule — **rates over one noun are selected by their words**, the same contrast test two plain quantities of one kind get. Money is one kind and things are many, which is what decides between them: two amounts of money over the same groups ALWAYS compose (dollars and dollars are not two kinds to choose between), and two rates over one thing never do. **Measured over all 3,004 problems: 463 answered, 462 right** (the one "wrong" is still the corpus error in row 76). Was 456. | Claude — **done** | three rows checked by hand first: 324,775 · 1,054,997 · 109,044 |
| 69 | **The digits deck was unreachable.** `decodeNormalForm` (rules/parse.ts) decoded Peano and int numerals but not the digit lists the `dig.*` deck normalises to, so every column-wise derivation decoded to null and became an ASK — the whole deck was dead weight. Fixed; the story engine now emits add / subtract / multiply in the digits deck and only division in Peano. Measured: "544 pots × 32 flowers" was 10,000+ steps and 29 seconds (an honest ASK after half a minute); it is now a few hundred steps, and the corpus's 407 readings reduce in 4.5 s total, max 3,819 steps. | Claude — done | the remaining Peano cost is division (~1 s for 1802 ÷ 34): a `dig.div` rule would finish the job |
| 70 | **Reference resolution corrupted the sentence it was asked about.** Found by the word-problems bench, which is the first place the observer answers hundreds of unrelated utterances in one session. `resolveReferences` substituted the previous turn's last content word for EVERY pronoun in the new utterance, so "how many more push-ups … than David" left "than" as the referent and the next question became "Katie had 72 DS games and **than** friend had 23 games. How many do **than** have total?" — which the observer then answered (49 instead of 95). Three fixes, all in `teacher/context.ts` with tests: grammatical vocabulary and common verbs can never be referents (a verb was being chosen too: "what about **saw**?"); a referent the observer does not know is not substituted at all; and memory is consulted only for a pronoun with nothing before it — "A pet store had six kittens. If **they** got another three" names its own subject, and an expletive "it is 78 miles to Grandma's house" refers to nothing. | Claude — **done** | it cost 3 of the bench's 4 remaining wrong answers |
| 71 | **What the story engine still declines, in its own words** (each is a decline, not a wrong answer, and each is a task): a sum across kinds that needs the store ("35 elephants and 48 tigers — how many legs", "308 girls and 318 boys — how many pupils" → #65); a difference between a whole and a part ("together their strawberries weighed 26, Marco's 10 — how much more did his dad's weigh"); a transfer between two owners; a question that selects by exclusion ("how many did she find AFTER monday"); geometry, proportion, ratio, doubling, LCM and unit conversion; a story that hides the number the answer needs; a payment and its change ("$5.00 to buy an airplane that costs $4.28 — how much change will he get?": "change" is not a noun the story counts, and reading it as a total would ADD the two — the shape is `paid − cost` and it needs its own reading); and a question naming a kind the story never states unless it is the missing-part shape. | Claude | the bench's `needs` buckets say which to build next |

**The training data, and whether it is actually wired in (2026-09-09 audit)**

| # | Task | Who | Note |
|---|---|---|---|
| 72 | **The audit.** What is on disk against what the classroom reads, and what the live record has actually ingested: conceptnet 26,000 of 71,949 rows (36%), passages 24,000 of 39,319 (61%), problems **3,004 of 3,004 — exhausted**, dialogue **never produced** (the fetch kept 0 rows), definitions **never produced** (a reader with no fetcher since the day it was written) while 9,039 ConceptNet-grown words sat with `definition: ''`. Written up in docs/CURRICULUM.md; the standing check is task 73. | Claude — done | huggingface.co is 403 through the egress allowlist from both the cloud and the desktop VM, so nothing new could be downloaded — the fixes below needed none |
| 73 | **A bench that fails an inert corpus.** `curriculum/corpusWiring.test.ts` walks the real corpus directory and reports, per source, what ONE feed changes in the observer — deck words, graph edges, exchanges, grades — and fails a source that changes nothing and grades nothing. It also names every reader with no file. This is the check that would have caught all four findings above on the day they appeared. | Claude — done | `OBSERVER_CORPUS`, `WIRING_BUDGET`; measured: conceptnet +2 edges/+1 word, definitions 27 defined/+27 words/+6 edges, passages +6 edges, problems 2 right 0 wrong 25 abstained and wraps into pass 2 |
| 74 | **Practice recycles; knowledge does not.** The problems cursor wraps at the end of the corpus (a smaller 50-row slice, since each row costs a full answer), so the arithmetic corpus keeps teaching and every improvement is re-measured against all 3,004 problems. Before this the corpus was spent once, by the parser that got 19 of 24 wrong, and was dead to learning for good. The three verdicts and the pass number are now in `TrainingStats` and the Introspect panel. | Claude — done | `curriculum/registry.ts` `PRACTICE_KINDS` |
| 75 | **The definitions corpus, from data already on disk.** `npm run extract-definitions` lifts 2,875 glosses out of the 19,324 Simple English Wikipedia passages we had already fetched ("air: the Earth's atmosphere"), 318 of them filling grown words that had none. The adapter now also ADMITS the word it defines — the deck grows from ConceptNet with empty definitions anyway, so a row that brings a gloss is better vocabulary than the empty slot. An authored gloss is never overwritten. | Claude — done | `cli/extract-definitions.ts`; the file is operator data (corpus/ is gitignored) and lives on the Mac |
| 76 | **The feeder's stale vocabulary cache.** The deck vocabulary the ConceptNet adapter filters against was computed once per feeder, so a word learned through any other channel — a read passage, a definition, the chaperone — was invisible to it for the life of the loop, and rows about that word were skipped silently (a skipped row looks exactly like a row about two unknown words). Rebuilt whenever the deck size changes; growth is also capped per feed (400 words) so one feed cannot front-load thousands of word-only states. | Claude — done | |
| 77 | **`dialogue.jsonl`** — fetched 2026-09-09 from the `pixelsandpointers/better_daily_dialog` mirror through the utterance-grouping converter: 20,000 utterance rows → **3,761 exchange pairs**. The conversation channel now has data for the first time, and the feeder teaches each pair and drills it once (measured: 30 rows → 27 exchanges). | Sebastian ran the fetch — done | the 429 ladder and the rename-following in `cli/fetch-hf.ts` did their job; 925 s for the run |
| 81 | **A digit-backed rational, so money with cents can be derived.** The rationals deck is exact but rides UNARY numerals: `$53.90 − $39.33` cross-multiplies 539/10 and 3933/100 into tens of thousands of successors, so the operand ceiling (`MAX_OPERAND`, 2,000 on numerator and denominator) declines it. That ceiling is why only 2 of the corpus's ~90 decimal problems are answered today — the deck proves the arithmetic, the numerals make it unaffordable. Two ways in, and they compose: a `dig.div` rule (already wanted by #69) plus `rat.*` rules over digit lists rather than Peano numerals; or a scaled-money reading that multiplies every quantity by the common power of ten, derives in the digits deck, and returns `rat.q(<digit result>, 100)` — which needs the same digit-list rationals to decode. Until then the decline is coverage, not a wrong answer. | Claude | ~90 corpus rows are money with cents; also the honest route to percent, speed and density |
| 78 | **`fetch-curriculum.ts` writes orphans.** It saves `corpus/<subject>/<slug>.txt` trees that `discoverSources` cannot see (it matches five exact filenames and never recurses), so anything fetched that way has never reached the classroom. Either emit `passages.jsonl` rows or teach the registry to walk subject directories. | Claude | |

| 79 | **The creative layer was composing word-play at arithmetic.** Found the moment the dialogue corpus was ingested and the creative layer unlocked: a word problem the story engine declines does not parse, so it was not a "grounded question", and step 3 composed about it — inside the honest ungrounded frame ("these are only words I put together: 'Melissa scored 120 points…'"), but echoing the problem's own digits back. Any grader reading for a number takes that for an answer. Fixed in `TeacherAgent.chatAnswer`: an utterance that states quantities and asks how many or how much is a grounded question, so it routes to ASK. **And the bench was grading those declines as wrong answers** — 24 "wrong" on 60 problems, 10 of which had offered no number at all; `checkProblem` now counts an ungrounded composition and a reply with no number as abstentions, which is what they are. After both: **right 14 · wrong 0 · abstained 66** with all four knowledge corpora ingested (was 3 right / 10 wrong / 14 abstained). | Claude — done | this is what the wiring bench is for: the observer only misbehaved once the corpus was in it |

| 80 | **Colourize the training feed.** The learning stream is where you watch the observer learn and it was 80 lines of one grey: "conceptnet: 1000 rows · 43 new edges · 0 denied · 12 negations · 380 new words · 958 skipped · 45919 left · 612 ms" — the numbers that matter reading exactly like the ones that do not. `learning/logColors.ts` colours each ` · ` part by what it SAYS (emerald gained, rose wrong, amber declined/skipped, fuchsia the entropy readout, sky progress, dim grey for timings), bolds the count and leaves its words in the part's tone, and **dims any count of zero whatever it says** — "0 wrong" is the good news and must not read like a failure; a signed entropy delta is coloured by DIRECTION, since falling is learning and rising is new vocabulary or new disagreement. The gutter now carries the event kind's dot and label from `EVENT_STYLES`, a palette that had been written and wired to nothing. An error event tints its whole row. Two data-loss bugs fixed on the way: the client stamped the ARRIVING BATCH's time onto all 80 retained rows on every batch (so every line showed the newest clock reading), and it dropped `detail` and `score` before they reached the UI. | Claude — done | `learning/logColors.test.ts` (12 green) tests the judgment, not the palette |

**The live UI stopped being live (2026-09-09)**

| # | Task | Who | Note |
|---|---|---|---|
| 83 | **A readout that could not notice its own silence.** The model-state strip froze — field coherence, knowledge entropy, vocabulary, drives — while the app went on looking awake. Three faults, each of which alone was enough. (1) The state poll was gated on `status === 'ready'`: one failed GET set `error`, React tore the interval down, and nothing was left to notice the server had come back. The dev server restarts on every source edit, so this fired constantly. Now the poll runs whenever the hook is connected at all, and a successful read clears the error. (2) `remoteAvailable` went true once and stayed true, so the re-probe that reconnects the stream only ran when the app had NEVER reached the server. It now runs whenever the connection is in error, and the connect effect depends on the probe epoch, so every probe rebuilds the stream. (3) A half-open EventSource (a slept laptop, a restarted server the browser never re-reached) looked exactly like a quiet one, so a watchdog now rebuilds the stream when the server says it is ticking or training and no event has arrived for 30 s. And the strip carries the age of its own reading: after 12 s it says **stale** with the age beside it, dims, and stops presenting the tick stream as live. A frozen number that looks current is the one thing this project must not do with a readout. | Claude — **done** | `server/useRemoteObserver.ts` (`stateAt`), `App.tsx`, `components/ModelStateBar.tsx` |
| 84 | **One bad cycle ended the classroom.** `TrainingLoop.run` had a single `try` around the whole `while`, so the first exception anywhere in a cycle — one malformed corpus row, one step that threw — fell out of the loop, emitted "learning stopped", and the observer sat idle until the process was restarted; the UI showed `trainingRunning: false` and nothing said why. A cycle that throws is now reported on the feed and RETRIED, with a one-second backoff, and only `MAX_CONSECUTIVE_FAILURES` (5) in a row stops the loop — saying so in as many words. | Claude — **done** | `server/trainingLoop.ts` |
| 85 | **THE SERVER WASN'T DOWN — IT WAS DEAF.** The app showed `offline` and every number as "—" while the server was healthy and training. Measured on the live record (2026-09-10): `model.json` **134 MB**, `traces.json` **154 MB**, `word-states.json` **118 MB** — 395 MB in `data/` — and every save was `JSON.stringify` + **`writeFileSync`**, both synchronous, on a 30-second autosave. The HTTP server answers nothing while that runs, so several times a minute the browser's reachability probe — which allowed **1.5 s** — timed out, the app declared the server absent, tore the stream down and blanked the strip. Three changes: both save paths now `await writeFile`/`rename` so the write half hands the loop back; the probe allows 10 s, because a server that answers in three seconds is BUSY, not absent; and one missed probe no longer means anything — two in a row does. The status tooltip now also reports how long the last snapshot took, so the pause has a name. | Claude — **done** | `server/FilePersistenceStore.ts`, `server/ServerSession.ts`, `server/client.ts`, `App.tsx` |
| 86 | **The snapshot still blocks on `JSON.stringify`.** #85 fixed the write; serialising 134 MB in one call still stops the event loop for seconds, every autosave. Three ways out, in increasing order of work: save the model snapshot on a much longer cadence than the teacher's own flush (it is bookkeeping, not the record of record); stream the record out incrementally (`JSON.stringify` per top-level section, written as it goes); or serialise on a worker thread. Until one of them lands, the server is deaf for a few seconds every autosave and the strip will say `stale` while it happens — which is now at least honest. | Claude | the numbers to beat: 134 MB, ~30 s cadence |
| 87 | **The server now reports its own responsiveness, because a busy server and an absent one looked identical.** The observer and the HTTP server share one thread, so every synchronous step — a snapshot write, a 1,000-row corpus feed and the graph rebuild behind it, the network-entropy sweep over every concept — is time no request can be answered. From the browser that is indistinguishable from a server that has gone away, which is how a whole afternoon went into looking for a bug in the UI. `ServerSession` now runs a 250 ms metronome whose DRIFT is the lag, reports the worst value of each rolling minute on the state line as `loop.maxLagMs`, and the strip shows `busy 4.2s` beside the status when it exceeds a second. The training loop also yields (`setImmediate`) before and after each heavy step, so the browser gets a turn between them rather than waiting out a whole cycle. `SERVER_BUILD` → 2026-09-10.1. | Claude — **done** | the yields do not make the steps shorter; #86 and a smaller feed budget are what would |

**Ongoing**

| # | Task | Who |
|---|---|---|
| 57 | Share of grades from checkable sources tracked in the record; LLM grades never outvote a checkable source | Claude |
| 58 | Retire the dead paths: `teacher/critic.ts`, substrate-entropy consolidation and field recall term as optional modes | Claude |

## Housekeeping

| # | Task | Who |
|---|---|---|
| 24 | Delete `_to_delete/` at the repo root (git lock files the sandbox couldn't remove) | Sebastian |
| 25 | Remaining defects from ANALYSIS.md §6: #10 (ask-layer frames count as abstentions — folds into #17), #11 (chat evidence can't promote hypotheses), #13 (story parser — folds into #22), #14 (duplicate `case` labels in `dsl.ts`), #17 (stale constants registry pointers), #18 (wall-clock assertions in unit tests), #19–20 (fuzz bench seeding/denominator — superseded by the null-arm bench) | Claude |
| 26 | Remove the dead duplicate critic `teacher/critic.ts` (no importers) | Claude |
| 27 | Make the repo runnable off-Mac: `npm ci` instead of a checked-in `node_modules` with darwin-only binaries | Sebastian (optional) |
